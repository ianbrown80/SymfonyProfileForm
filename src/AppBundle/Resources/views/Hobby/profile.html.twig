{% extends 'base.html.twig' %}
{% block body %}
  <a class="btn btn-primary" href="/">Back to User List</a>
  <hr>
  <h2 class="page-header">{{ user.name }}</h2>
  <div class="col-md-6">
    <img href="{{ asset('img/profile-placeholder.jpg') }}"
  </div>
  <div class="col-md-6">
    <ul class="list-group">
      <li class="list-group-item">Description: {{ user.description }}</li>
    <ul>
  </div>
{% endblock %}
