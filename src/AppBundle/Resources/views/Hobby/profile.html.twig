{% extends 'base.html.twig' %}
{% block body %}
  <a class="btn btn-primary" href="/">Back to User List</a>
  <h2 class="page-header">{{ user.name }}</h2>
  <div class="col-md-6">
    <img src="{{ asset('img/profile-placeholder.jpg') }}" />
    <p class="biography">{{ user.biography }}<//p>
  </div>
  <div class="col-md-6">
    <h3 class="page-header">Hobbies</h3>
  </div>
{% endblock %}
